#include <iostream>
#include "Sort.hpp"
#include "Employee.hpp"
#include <vector>
#include <algorithm>

int main(){
    // int v[] = {3, 7, 4, 9};
    // int v[] = {7, 3, 20, 15, 13, 14};
    // int v[] = {3, 7, 7, 4, 9, 5, 2, 6, 1};
    // Sort::selectionSort(v, 6);
    // Sort::insertionSort(v, 8);
    // Sort::quickSort(v, 0, 4);
    // Sort::mergeSort(v, 0, 8);
    // Sort::stableSelectionSort(v, 9);

    /* for(int i = 0; i < 9; i++){
        std::cout << v[i] << " ";
    }
    std::cout << "\n";*/

    //std::vector<Employee> v;
    //v.push_back(Employee("dlebson", "cardoso", "0"));
    //v.push_back(Employee("clebson", "cardoso", "1"));
    //v.push_back(Employee("clebson", "alves", "2"));
    //v.push_back(Employee("clebson", "de", "3"));
    //v.push_back(Employee("clebson", "sa", "4"));
    //v.push_back(Employee("aisele", "cardoso", "5"));

    return 0;
}
